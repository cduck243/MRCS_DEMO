# MRCS 생애위험준비지도 — 실행 방법

살면서 생길 수 있는 질병과 사고의 위험을 확인하고, 지금 준비가 어디까지 되어 있는지
보여 주는 서비스의 **예시 화면**입니다.

---

## 가장 쉬운 방법 — 그냥 열기

`index.html` 을 더블클릭하면 됩니다. 설치할 것도, 인터넷도 필요 없습니다.

다섯 화면이 이어져 있어 1단계부터 끝까지 볼 수 있습니다.
`AI로 다시 쓰기` 버튼만 나타나지 않고, 나머지는 모두 동작합니다.

---

## AI 문장 기능까지 보려면

5단계의 **"AI가 읽은 내 준비지도"** 를 로컬 AI가 다시 쓰게 할 수 있습니다.
문장은 이 컴퓨터 안에서 만들어지고 **밖으로 나가지 않습니다.**

### macOS

**`start.command` 를 더블클릭**하면 됩니다.

> 처음에는 "확인되지 않은 개발자" 경고가 뜰 수 있습니다.
> 파일을 우클릭 → **열기** → **열기** 를 누르면 실행됩니다.

모델이 없으면 받을지 물어봅니다 (약 5GB, 한 번만).
받고 나면 브라우저가 열리고, 5단계에서 `AI로 다시 쓰기` 를 누를 수 있습니다.

### 직접 실행 (Windows · Linux 포함)

미리 [Ollama](https://ollama.com) 를 설치하고 모델을 받아 둡니다.

```bash
ollama pull qwen3:8b
python3 tools/serve.py
```

브라우저에서 http://localhost:8800 을 엽니다.

```bash
python3 tools/serve.py --port 9000          # 포트 바꾸기
python3 tools/serve.py --model qwen3:8b     # 모델 바꾸기
```

---

## 다섯 단계

| | 화면 | 하는 일 |
|---|---|---|
| 1 | 지도 이해 | 위험을 수용·전가·회피로 나눠 보는 관점 |
| 2 | 위험 이해 | 질병 18개 분야 · 212개 질병군에서 선택 |
| 3 | 수준 결정 | 소득·지출·비상자금과 위험인내수준 |
| 4 | 준비지도 | 필요 금액과 준비 금액 비교 |
| 5 | 대안 확인 | 먼저 준비할 5가지 · AI 분석 · 보장리포트 |

각 화면 아래 **예시 조건 바꿔 보기** 에서 성별과 위험인내수준을 바꾸면
숫자가 어떻게 달라지는지 볼 수 있습니다.

---

## 폴더 구성

```
index.html          화면 목록
step1~5.html        5단계 화면 (데이터를 안에 품고 있어 단독 실행)
data/               정제된 원본 데이터
docs/               개발 사양서 — 계산 로직 · 표시 데이터 · 입력 포맷
tools/serve.py      로컬 서버 + AI 연결
tools/ai_brief.py   프롬프트 · 검사기 · 폴백
start.command       macOS 실행 파일
```

---

## 확인 안내

예시용 시연 화면입니다.

- 질병 설명 212건은 **의료전문가 검수 필요** 상태입니다.
- 보유 담보와 금액은 **가상의 예시**입니다. 실제 서비스에서는 신용정보원 보험정보 조회로 대체됩니다.
- 경과 시나리오 배수(1.0 / 2.0 / 4.0)는 **가정값**입니다.
- 특정 보험회사의 상품을 권유하지 않습니다.
- 정확한 진단과 치료 방법은 의료기관과 의료진의 확인을 받아야 합니다.

AI가 쓴 문장은 **검사기를 통과한 것만** 화면에 올라갑니다.
숫자를 지어내거나, 상품을 지목하거나, 겁을 주는 표현이 섞이면 버리고
기본 문장을 그대로 씁니다. 모델이 없거나 꺼져 있어도 화면은 정상 동작합니다.

---

헤인파트너스(주) · https://github.com/cduck243/MRCS_DEMO
